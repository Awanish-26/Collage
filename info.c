// outpot format specifier format  $ %[flags][width][.precision][length]specifier

// bool in not pre imorted in c , to import it $ #include <stdbool.h>

// Scanf accept only single word to get full line of text use $ fgets(<fullName>, sizeof(<fullName>), stdin);

// Use the scanf() function to get a single word as input, and use fgets() for multiple words.
